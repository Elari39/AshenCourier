-- ============================================================
-- 000002（down）：严格互逆 —— 与 up 的顺序相反
--   1. 先删索引，再删列（列被索引引用时删不掉）
--   2. 索引名与 up 完全一致，且不带 IF EXISTS：down 时索引必须真的在，
--      否则说明库的状态与迁移历史已经不一致，早报错比静默通过好
-- ============================================================

DROP INDEX click_events_event_uid_key;

ALTER TABLE click_events DROP COLUMN event_uid;