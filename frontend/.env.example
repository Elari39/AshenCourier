# ============================================================
# 前端环境变量样例
#
# ⚠️ 只有 VITE_ 前缀的变量会打进前端产物，任何人都能从 JS 里读出来。
#    绝不要把任何密钥放进来。
#
# 本地开发（pnpm dev）时如需覆盖，复制成 .env.local。
# 生产构建不需要任何变量：nginx 同源反代 /api，短链前缀由后端拼进 short_url 返回。
# ============================================================

# API 基址。留空/不设时用默认值 /api（同源反代）
# VITE_API_BASE_URL=/api

# 短链基址。**只在落地页演示区用到**（用户还没建任何链接、没有后端响应可展示时）。
# 真实链接的 short_url 一律由后端拼好下发，前端不自己拼 —— 这条只回答
# 「那段演示该显示什么域名」。留空/不设时回退 window.location.origin（同源部署）。
# 短链域名与控制台域名分离时才需要配它。⚠️ 改它要重新 build 镜像。
# VITE_SHORT_BASE_URL=https://s.example.com
