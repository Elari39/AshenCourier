# ============================================================
# 仓库根目录的 .dockerignore
#
# frontend 服务的构建上下文是仓库根目录（镜像需要 frontend/ + deploy/nginx/），
# Docker 只认「上下文根目录」下的 .dockerignore，所以这份文件必须放在根目录。
# backend 服务用的是 backend/.dockerignore。
# ============================================================

**/node_modules
**/dist
**/.vite
**/bin

# GeoIP 国家库：只挂进 backend / worker 容器，前端镜像用不到。
# 不排除的话，一份 9MB 的 mmdb 会白白进 frontend 的构建上下文。
deploy/geoip

# 浏览器级验收脚本：只在本机/CI 的 runner 上跑，不进镜像。
# 不排除的话，改一行 e2e 也会让 frontend 镜像的构建缓存失效。
frontend/e2e

.git
.gitignore
.gitattributes
.workbuddy

# 文档与开发辅助文件不进构建上下文
README.md
DESIGN.md
PLAN.md
docker-compose.yml
docker-compose.dev.yml

*.log
*.exe
*.test
*.out

.env
.env.*
!.env.example
!frontend/.env.example
